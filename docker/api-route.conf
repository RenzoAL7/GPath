# Fail explicitly if the ingress accidentally sends API traffic to the web.
location /api {
    default_type application/json;
    set $gpath_cache_control "no-store";
    return 503 '{"error":"API routing is not configured on this web server"}';
}
